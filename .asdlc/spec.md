# Overview

An API that returns "Hello, World!" when called.

# Personas

- Developer — calls the API to verify it's working.

# Features

- A developer makes a request to the API.
- The API responds with "Hello, World!".